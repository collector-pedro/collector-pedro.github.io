// app.js
const { createApp, ref, computed, onMounted, watch } = Vue;

const app = createApp({
    setup() {
        // TODO: Cole todo o código Vue que está dentro das <script> do index.html aqui
        // (o conteúdo entre "const { createApp..." até "app.mount('#app')")
    }
});

app.mount('#app');